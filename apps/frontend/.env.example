# Local Development Environment Variables
# Copy this file to .env.local — .env.local is gitignored and NEVER committed to version control.
# NEVER store secrets, API keys, or passwords in .env (which is committed to git).

# Backend API base URL — change in staging/production deployments
VITE_API_BASE_URL=http://127.0.0.1:8000/api
